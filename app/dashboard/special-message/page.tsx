import { SpecialMessagesClient } from "@/components/special-messages/special-messages-client"

export const metadata = {
  title: "Special Message | Legacy Keeper",
  description: "Manage and share special messages with others",
}

export default function SpecialMessagePage() {
  return <SpecialMessagesClient />
}
